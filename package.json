{
  "name": "shoe-try-on",
  "version": "1.0.0",
  "description": "Embeddable DeepAR shoe try-on with a bottom product slider.",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && webpack --config webpack.config.js --mode production && cp -r public/* dist && cp -r node_modules/deepar dist/deepar-resources",
    "dev": "webpack serve --mode development --open --port 9000"
  },
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.4.5",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.11.0"
  },
  "dependencies": {
    "deepar": "^5.0.0"
  }
}
